name: CI Legacy

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  test:
    name: legacy-test-disabled
    runs-on: ubuntu-latest
    steps:
      - name: Legacy workflow disabled
        run: |
          echo "This legacy CI workflow is intentionally disabled."
          echo "Use .github/workflows/ci.yml for active CI checks."
