name: CI Legacy Disabled

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    name: legacy-ci-disabled
    runs-on: ubuntu-latest
    steps:
      - name: Legacy workflow disabled
        run: |
          echo "This obsolete legacy workflow is disabled on refactor/misc."
          echo "Use .github/workflows/ci.yml on main for active CI."
